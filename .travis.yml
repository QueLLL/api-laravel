language: php

php:
  - 5.6
  - 7.0

before_script:
  - cd laravel
  - cp .env.example .env
  - composer install
  - php artisan key:generate

script:
  - vendor/bin/phpunit
  - vendor/bin/phpstan analyse